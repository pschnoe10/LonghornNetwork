import java.io.*;
import java.util.*;

/**
 * DataParser is a class that is used to parse data from the inputted filename.
 *
 */

public class DataParser {
    /**
     *
     * @param filename is the inputted string that needs to be parsed.
     * @return an ArrayList of type UniversityStudent with the parsed inputted string.
     * @throws IOException
     */
    public static List<UniversityStudent> parseStudents(String filename) throws IOException {
        return new ArrayList<>();
    }
}
