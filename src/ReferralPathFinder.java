import java.util.*;

/**
 * ReferralPathFind is the class that contains the methods used to implement Dijkstra's algorithm to find the shortest path to an internship.
 *
 */
public class ReferralPathFinder {
    /**
     *
     * @param graph is the inputted graph that is used to find the shortest referal path to an internship.
     */
    public ReferralPathFinder(StudentGraph graph) {
        // Constructor
    }

    /**
     *
     * @param start is the referal node in the graph where we are starting from.
     * @param targetCompany is the node we want to end up at where our target internship is.
     * @return an ArrayList of which path we should take for the shortest amount of references to the target company.
     */
    public List<UniversityStudent> findReferralPath(UniversityStudent start, String targetCompany) {
        // Method signature only
        return new ArrayList<>();
    }
}
